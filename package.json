{
  "name": "arisen-ios-browser-signature-provider-interface",
  "version": "1.0.0",
  "description": "A signature provider that enables iOS applications to route signing requests to an iOS-based authenticator app.",
  "author": {
    "name": "arisen",
    "url": "https://arisen.network"
  },
  "contributors": [
    "shikhar srivastava"
  ],
  "license": "MIT",
  "homepage": "https://github.com/ARISENIO/arisen-ios-browser-signature-provider-interface#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ARISENIO/arisen-ios-browser-signature-provider-interface.git"
  },
  "bugs": {
    "url": "https://github.com/ARISENIO/arisen-ios-browser-signature-provider-interface/issues"
  },
  "keywords": [
    "signature",
    "provider",
    "interface",
    "bridge",
    "rix",
    "arisen",
    "arisenjs"
  ],
  "main": "dist/index.js",
  "scripts": {
    "build": "yarn lint & tsc -p ./tsconfig.json",
    "clean-build": "rm -rf dist && yarn build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "tslint -c tslint.json src/*.ts",
    "prepublish": "yarn clean-build"
  },
  "devDependencies": {
    "@blockone/tslint-config-blockone": "^4.0.1",
    "@types/qs": "^6.5.1",
    "tslint": "^5.11.0",
    "tslint-eslint-rules": "^5.4.0",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "arisen-signature-provider-interface": "^0.0.1",
    "qs": "^6.5.2",
    "uuid": "^3.3.2"
  }
}
